create table authors (
                         author_id integer not null,
                         name text,
                         surname text,
                         description text,
                         addition_date date,
                         primary key (author_id)

);