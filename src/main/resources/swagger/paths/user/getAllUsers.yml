get:
  summary: Get all Users
  operationId: getAllUsers
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../models/user.yml#/components/schemas/UserDTO'