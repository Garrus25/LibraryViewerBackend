get:
  tags:
    - Autor
  summary: Pobierz najnowszych autorów
  operationId: getNewlyAddedAuthors
  parameters:
    - name: amount
      in: path
      required: true
      schema:
        type: integer
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../models/author.yml#/components/schemas/AuthorDTO'
    400:
      description: BAD REQUEST
      content:
        application/json:
          schema:
            type: string