get:
  tags:
    - Autor
  summary: Pobierz wszystkich autorów
  operationId: getAllAuthors
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../models/author.yml#/components/schemas/AuthorDTO'