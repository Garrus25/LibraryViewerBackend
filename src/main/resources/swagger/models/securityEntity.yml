components:
  schemas:
    SecurityEntity:
      type: object
      description: Obiekt Security
      properties:
        token:
          type: string
        refreshToken:
          type: string
        id:
          type: string